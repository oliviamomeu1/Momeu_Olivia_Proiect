@page
@model Momeu_Olivia_Proiect.Pages.Furnizori.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Furnizori</h1>

<p>
    <a asp-page="Create">Adauga</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Furnizor[0].NumeFurnizor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Furnizor) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumeFurnizor)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Editeaza</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Detalii</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Sterge</a>
            </td>
        </tr>
}
    </tbody>
</table>
